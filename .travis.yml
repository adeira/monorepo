language: php

php:
  - 7.0
  - 7.1

before_script:
  - composer selfupdate --no-progress
  - composer install --prefer-dist
  - git submodule update --init

script:
  - ./run-tests.sh

notifications:
  email: false
